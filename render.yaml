services:
  - type: static
    name: youssef-portfolio-2026
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: out
    # routing for SPAs if needed (though Next.js export handles most)
    routes:
      - type: rewrite
        source: /index.html
        destination: /
